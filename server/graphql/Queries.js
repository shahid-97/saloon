const _ = require('lodash');
const moment = require('moment');

const products = async data => {
    try {
        
        return
    } catch (error) {
        console.error(error);
        return error;
    }
}


module.exports = { products }